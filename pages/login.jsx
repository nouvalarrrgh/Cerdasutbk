import { useState } from 'react'; import { useRouter } from 'next/router';
export default function Login(){ const [email,setEmail]=useState(''); const [pw,setPw]=useState(''); const router=useRouter();
const submit=(e)=>{ e.preventDefault(); if(email && pw){ localStorage.setItem('loggedIn','true'); localStorage.setItem('user',email); router.push('/'); } else alert('isi email dan password'); };
return (<div className="min-h-screen flex items-center justify-center"><div className="card max-w-md"><h1 className="text-2xl font-bold text-emerald-700 mb-4">Masuk CerdasUTBK</h1><form onSubmit={submit} className="space-y-3"><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="email" className="w-full p-3 border rounded-lg"/><input value={pw} onChange={e=>setPw(e.target.value)} type="password" placeholder="password" className="w-full p-3 border rounded-lg"/><button className="w-full py-2 bg-emerald-600 text-white rounded">Masuk</button></form></div></div>); }
